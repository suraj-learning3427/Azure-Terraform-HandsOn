{
  "name": "azure-devops-terraform-training-program",
  "version": "1.0.0",
  "description": "Azure DevOps and Terraform Enterprise Training Program — validation scripts and property tests",
  "scripts": {
    "validate": "ts-node src/validate-structure.ts",
    "test": "jest --runInBand",
    "test:properties": "jest --testPathPattern=properties --runInBand"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.7",
    "fast-check": "^3.19.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
